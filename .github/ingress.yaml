apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: picsrate-ui-ingress
  namespace: studio-qa
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
  
spec:
  rules:
    - host: picsrate-ui.bbs-studio.tn
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: picsrate-ui-svc
              port:
                number: 80